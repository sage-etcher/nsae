
stb_ds_lib = static_library('stb_ds', [
    'stb_ds.c',
  ],
  dependencies : [
    stb_dep,
  ],
)

stb_ds_dep = declare_dependency(
  link_with : stb_ds_lib,
  dependencies : stb_dep,
)

# end of file
